local interface = require "org.ryuu.loop.lang.keyword.interface"

---@class Collection
local Collection = interface("Collection")

---@return number
function Collection:Count()
end

return Collection
